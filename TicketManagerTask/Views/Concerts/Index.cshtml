@page
@model TicketManagerTask.Views.Concerts.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Concert[0].Singer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Concert[0].Location)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Concert[0].Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Concert[0].Discriminator)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Concert) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Singer.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Location.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Discriminator)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
